# colcon build artifacts
ws/build/
ws/install/
ws/log/

# limo_ros2 is a clone with its own git repository; the fixes applied to it
# live in patches/limo_ros2-fixes.patch, which is versioned here
ws/src/limo_ros2/

# gerado pelo setup.sh, com os IDs desta maquina
.env
